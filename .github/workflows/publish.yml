name: Deployment

on:
  push:
    branches:
      - main
      - stg
      - preprod
jobs:
  publish-image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Build image
        run: docker build . -t an6eel/gitops-seminar-code:$GITHUB_SHA

      - name: Log into registry
        run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}

      - name: Push image
        run: docker push an6eel/gitops-seminar-code:$GITHUB_SHA
  notify-update:
    needs: publish-image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Notify infra repo update
        run: |
          git clone --single-branch --branch main https://an6eel:$GITHUB_TOKEN@github.com/an6eel/gitops-seminar-infra
          case "${GITHUB_REF##*/}" in
            main) export REPO_PATH=prod ;;
            preprod) export REPO_PATH=preprod ;;
            stg) export REPO_PATH=stg ;;
          esac

          cd gitops-seminar-infra/resources/apps/api/$REPO_PATH
          docker run --rm -v ${PWD}:/workdir mikefarah/yq:3.2.1 yq write --inplace --verbose values.yaml image.tag $GITHUB_SHA
          git commit -am "Update code" && git push origin master



